while True:
    n = int(input("\nInserisci un numero positivo: "))
    
    if n <= 0:
        print("Inserisci un numero maggiore di 0!")
        continue
        
    i = n
    while i >= 0:
        print(i, end=" ")
        i -= 1
    print() # va a capo
        
    
